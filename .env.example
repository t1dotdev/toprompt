DATABASE_URL=postgres://user:password@host:5432/toprompt
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
# openssl rand -base64 32
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3000

# Public origin. Used for og:url and og:image so link previews resolve.
# Read at BUILD time, not at runtime — deploys must pass it as a build argument.
VITE_SITE_URL=http://localhost:3100
